<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Life Insurance That Builds a Legacy | Ray's Financial Group</title>

  <!-- Tailwind CSS -->
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- EmailJS SDK -->
  <script src="https://cdn.jsdelivr.net/npm/@emailjs/browser@4/dist/email.min.js"></script>
  <script>
    (function () {
      // ✅ Your actual EmailJS Public Key
      emailjs.init("twwP365c5kihIeOT7");
    })();
  </script>

  <style>
    body { font-family: 'Inter', sans-serif; }
    .hero-gradient { background: linear-gradient(135deg, #1e3a8a 0%, #065f46 50%, #b45309 100%); }
    .legacy-gradient { background: linear-gradient(135deg, #065f46 0%, #b45309 50%, #1e3a8a 100%); }
    .cta-button { background: linear-gradient(45deg,#b45309,#d97706); transition: all .3s ease; box-shadow: 0 4px 15px rgba(180,83,9,.3); }
    .cta-button:hover { transform: translateY(-2px); box-shadow: 0 8px 25px rgba(180,83,9,.4);}
  </style>
</head>

<body class="bg-white">
  <!-- Header -->
  <header class="bg-white shadow-lg sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4 flex justify-between items-center">
      <h1 class="text-xl font-bold text-blue-900">Ray's Financial Group</h1>
      <a href="tel:386-400-5886" class="cta-button text-white px-4 py-2 rounded-lg font-bold">📞 (386) 400-5886</a>
    </div>
  </header>

  <!-- Hero -->
  <section class="hero-gradient text-white py-24 text-center">
    <h2 class="text-4xl md:text-6xl font-black mb-6">Life Insurance That Builds a Legacy</h2>
    <p class="text-lg md:text-xl max-w-2xl mx-auto mb-10">Protect your family, grow wealth, and leave a legacy through Key Person Insurance, IUL, Whole Life, and more.</p>
    <button onclick="scrollToSection('quote-form')" class="cta-button text-white px-8 py-4 rounded-xl text-lg font-bold">🎯 Get My Free Quote</button>
  </section>

  <!-- Quote Form -->
  <section id="quote-form" class="py-20 bg-blue-900">
    <div class="max-w-3xl mx-auto px-4">
      <div class="bg-white rounded-2xl shadow-2xl p-8">
        <h3 class="text-2xl font-bold text-center mb-6">See What Your Family Qualifies For</h3>

        <form id="quote-request-form" class="space-y-6" onsubmit="handleFormSubmit(event)">
          <!-- Honeypot -->
          <input type="text" name="company" id="company" tabindex="-1" autocomplete="off" class="hidden" aria-hidden="true">

          <div class="grid md:grid-cols-2 gap-6">
            <div>
              <label for="firstName" class="block font-semibold mb-2">First Name *</label>
              <input id="firstName" name="firstName" type="text" required class="w-full px-4 py-3 border rounded-lg"/>
            </div>
            <div>
              <label for="lastName" class="block font-semibold mb-2">Last Name *</label>
              <input id="lastName" name="lastName" type="text" required class="w-full px-4 py-3 border rounded-lg"/>
            </div>
            <div>
              <label for="email" class="block font-semibold mb-2">Email *</label>
              <input id="email" name="email" type="email" required class="w-full px-4 py-3 border rounded-lg"/>
            </div>
            <div>
              <label for="phone" class="block font-semibold mb-2">Phone *</label>
              <input id="phone" name="phone" type="tel" required class="w-full px-4 py-3 border rounded-lg"/>
            </div>
          </div>

          <div>
            <label for="policyInterest" class="block font-semibold mb-2">Interested Policy *</label>
            <select id="policyInterest" name="policyInterest" required class="w-full px-4 py-3 border rounded-lg">
              <option value="">Select...</option>
              <option>Key Person Insurance</option>
              <option>IUL (Growth Insurance)</option>
              <option>Whole Life Insurance</option>
              <option>Term Life Insurance</option>
              <option>Not Sure</option>
            </select>
          </div>

          <!-- Consent -->
          <label class="flex items-start gap-2 text-sm">
            <input type="checkbox" id="consent" required class="mt-1">
            <span>By submitting, you agree to be contacted by phone, email, and text at the number provided. Message/data rates may apply. Consent is not a condition of purchase.</span>
          </label>

          <div class="text-center">
            <button type="submit" class="cta-button text-white px-8 py-4 rounded-lg font-bold">Submit Quote Request</button>
          </div>
        </form>

        <div id="form-message" class="mt-6 text-center text-gray-700"></div>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="bg-gray-900 text-white text-center py-6">
    <p>© 2024 Ray's Financial Group | Curtis Ray</p>
  </footer>

  <script>
    function scrollToSection(id) {
      document.getElementById(id)?.scrollIntoView({ behavior: 'smooth' });
    }

    function collectFormData(form) {
      const fd = new FormData(form);
      const data = {};
      for (const [k, v] of fd.entries()) data[k] = v;
      return data;
    }

    function handleFormSubmit(e) {
      e.preventDefault();
      const form = e.target;
      const data = collectFormData(form);

      if ((data.company || '').trim() !== '') return; // honeypot
      if (!document.getElementById('consent').checked) {
        alert("Please check the consent box to continue.");
        return;
      }

      document.getElementById("form-message").innerText = "⏳ Sending...";

      // ✅ Your actual Service ID and Template ID
      emailjs.send("service_veds8n5", "template_quote_form", {
        to_email: "sfg.cray@gmail.com",
        from_name: `${data.firstName} ${data.lastName}`,
        from_email: data.email,
        phone: data.phone,
        policy_interest: data.policyInterest,
        submission_date: new Date().toLocaleString(),
        reply_to: data.email
      }).then(() => {
        document.getElementById("form-message").innerText = "✅ Thank you! Curtis will contact you soon.";
        form.reset();
      }).catch(err => {
        console.error("EmailJS Error:", err);
        document.getElementById("form-message").innerText = "❌ There was an issue. Please call (386) 400-5886.";
      });
    }
  </script>
</body>
</html>
